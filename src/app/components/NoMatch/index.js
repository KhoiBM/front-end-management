import React from 'react';
export function NoMatch() {
    return (
        <div>
            <h3>
                {/* No match for <code>{location.pathname}</code> */}
                <p>NoMatch</p>
            </h3>
        </div>
    );
}
